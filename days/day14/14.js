const fs = require('fs');
const path = require('path');

const part1 = textFile => {
    const input = fs.readFileSync(path.join(__dirname, textFile), 'utf-8');
    return 0;
};

const part2 = (textFile) => {
    const input = fs.readFileSync(path.join(__dirname, textFile), 'utf-8');
    return 0;
};

module.exports = {
    day: 14,
    part1: part1,
    part2: part2,
};
